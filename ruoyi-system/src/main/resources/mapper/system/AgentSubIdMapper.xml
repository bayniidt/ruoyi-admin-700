<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.AgentSubIdMapper">

    <resultMap id="AgentSubIdResult" type="com.ruoyi.system.domain.AgentSubId">
        <id property="id" column="id"/>
        <result property="subid" column="subid"/>
        <result property="name" column="name"/>
        <result property="status" column="status"/>
        <result property="source" column="source"/>
        <result property="createdBy" column="created_by"/>
        <result property="createdByName" column="created_by_name"/>
        <result property="promoLink" column="promo_link"/>
        <result property="boundAt" column="bound_at"/>
    </resultMap>

    <select id="selectMySubIdList" resultMap="AgentSubIdResult">
        select id, subid, name, status, source, created_by, created_by_name, promo_link, bound_at
        from agent_subid
        where created_by = #{createdBy}
        order by id desc
    </select>

    <select id="selectSubIdListByUserIds" resultMap="AgentSubIdResult">
        select id, subid, name, status, source, created_by, created_by_name, promo_link, bound_at
        from agent_subid
        where created_by in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
        order by id desc
    </select>

    <select id="selectSubIdsByUserIds" resultType="String">
        select distinct s.subid
        from agent_subid s
        where s.status = 1
          and s.created_by in
        <foreach collection="userIds" item="userId" open="(" separator="," close=")">
            #{userId}
        </foreach>
          and (select count(1) from agent_subid all_subids
               where lower(all_subids.subid) = lower(s.subid)) = 1
    </select>

    <select id="countBySubid" resultType="int">
        select count(1)
        from agent_subid
        where lower(subid) = lower(#{subid})
    </select>

    <insert id="insertAgentSubId" parameterType="com.ruoyi.system.domain.AgentSubId" useGeneratedKeys="true" keyProperty="id">
        insert into agent_subid (
            subid, name, status, source, created_by, created_by_name, promo_link, bound_at, create_by, create_time
        ) values (
            #{subid}, #{name}, #{status}, #{source}, #{createdBy}, #{createdByName}, #{promoLink}, #{boundAt}, #{createBy}, sysdate()
        )
    </insert>

    <update id="updateSubIdOwner">
        update agent_subid
        set created_by = #{targetUserId},
            created_by_name = #{targetUserName},
            bound_at = sysdate(),
            update_by = #{updateBy},
            update_time = sysdate()
        where id = #{id}
        <choose>
            <when test="manageableOwnerUserIds != null and !manageableOwnerUserIds.isEmpty()">
                and created_by in
                <foreach collection="manageableOwnerUserIds" item="ownerUserId" open="(" separator="," close=")">
                    #{ownerUserId}
                </foreach>
            </when>
            <when test="manageableOwnerUserIds != null">
                and 1 = 0
            </when>
        </choose>
    </update>

</mapper>
